add = +
substact = -
multiply = *
division = /
power = pow(a,b)
remainder = a % 3

# include<stdio.h>
# include<stdio.h>

int main() {
    int a = (int) 1.22222;
    printf("%d \n", a);
    return 0;
}